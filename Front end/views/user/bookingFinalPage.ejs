<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Booking Confirmation</title>
</head>
<body>
    <!-- <header class="site-header py-3 bg-primary">
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container">
                <a class="navbar-brand" href="#">TravelNest</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="/userManagement">Profile Management</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/manageBookings">Manage Bookings</a>
                        </li>
                        <% if(user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/wallet">Wallet</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/sign_out">Sign Out</a>
                        </li>
                        <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Log in/Sign up</a>
                        </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
    </header> -->
<style>
    body {
    font-family: Arial, sans-serif;
    text-align: center;
    background-color: #f0f0f0;
    margin: 0;
}

.confirmation-container {
    max-width: 600px;
    margin: 50px auto;
    padding: 20px;
    background-color: #fff;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

h1, h2 {
    color: #333;
}

.booking-details, .contact-info {
    margin-bottom: 20px;
    text-align: left;
}

button {
    padding: 10px 20px;
    font-size: 16px;
    background-color: #007bff;
    color: #fff;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

button.print-button {
    background-color: #28a745;
}

button:disabled {
    background-color: #ccc;
    cursor: not-allowed;
}

</style>


    <div class="confirmation-container">
        <h1>Booking Confirmed!</h1>
        <div class="booking-details">
            <h2>Booking Details</h2>
            <p><strong>Name:</strong> <%=booking.name%></p>
            <p><strong>Email:</strong> <%=booking.email%></p>
            <p><strong>Accommodation:</strong> <%=room.roomType%></p>
            <p><strong>Dates:</strong> Check-in:<%=booking.checkin_date%> - Check-out: <%=booking.checkout_date%></p> 
            <p><strong>Total Cost:</strong>  <%=booking.otherDetails.moneyPaid + booking.otherDetails.pendingAmount %></p>
            <p><strong>Confirmation Number:</strong> <%=booking.roomNumber%></p>
        </div>
        <div class="contact-info">
            <h2>Contact Information</h2>
            <p>For any inquiries or assistance, please contact:</p>
            <p>Email:  <%=hotel.email%></p>

        </div>
        <a href="/manageBookings"><button class="print-button">Manage your Booking</button></a>
    </div>
</body>
</html>
